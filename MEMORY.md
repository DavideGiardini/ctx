# Architecture Memory → moved

The architecture decision log that used to live here has been migrated to
version-tracked ADRs. See **[`docs/decisions/`](docs/decisions/)**.

The five "deepening candidates" (ConversationCore, Provider protocol,
ConversationRepository, pure context builder, injected Workspace) are now
ADRs 0001–0005. Record new decisions there, not here.
