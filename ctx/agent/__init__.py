"""Agent integration: headless driving + semantic state snapshots for ctx.

This package lets coding agents drive and observe the app token-efficiently.
It is test/automation tooling, kept separate from the production UI/core code.
"""
